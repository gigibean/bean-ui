import { Meta } from '@storybook/addon-docs';
import Code from './assets/code-brackets.svg';
import Colors from './assets/colors.svg';
import Comments from './assets/comments.svg';
import Direction from './assets/direction.svg';
import Flow from './assets/flow.svg';
import Plugin from './assets/plugin.svg';
import Repo from './assets/repo.svg';
import StackAlt from './assets/stackalt.svg';
import { Loader, LoadingProps } from '@bean-ui/core';
import { colors, textColors } from '@bean-ui/common';
import { Button, BaseButtonProps } from '@bean-ui/core';

<Meta title="Common/Introduction" />

<style>{`
  li {
    list-style: none;
  }
  .subheading {
    --mediumdark: '#999999';
    font-weight: 900;
    font-size: 13px;
    color: #999;
    letter-spacing: 6px;
    line-height: 24px;
    text-transform: uppercase;
    margin-bottom: 12px;
    margin-top: 40px;
  }

  .link-list {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr 1fr;
    row-gap: 10px;
  }

  .color-palette {
    display: grid;
    grid-template-columns: 15% 85%;
    grid-template-rows: 1fr;
    width: 100%;
  }

  .color-palette span {
    display: inline-block;
    width: 60px;
    margin: 0.5px 0.5px;
    font-size: 10px;
  }

  .palette span  {
    display: inline-flex;
    justify-content: flex-end;
    align-items: flex-end;
  }

  .color-labels, .color-palette > div {
    margin: 0;
    display: grid;
  }

  .color-palette span, .color-palette li {
    height: 60px;
  }

  .palette {
    display: grid;
    grid-template-rows: repeat(18, 1fr);
  }

  .palette b {
      display: none;
    }

  .color-shades {
    display: flex;
    align-items: flex-end;
    border-bottom: 1px solid #eee;
    height: 48px;
    padding-left: 15%;
  }

  .color-shades > li {
    display: flex;
    align-items: conter;
    justify-content: center;
    flex-direction: column;
    height: 100%;
    line-height: 16px;
    margin: 0 0.5px;
    width: 60px;
    text-align: center;
  }

  .color-labels li {
    margin: 0.5px 0;
    align-items: center;
    display: flex;
    min-width: 0;
    padding-left: 8px;
    position: relative;
    height: 60px;
  }

  @media (min-width: 620px) {
    .link-list {
      row-gap: 20px;
      column-gap: 20px;
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 1005px) {
    .color-palette > div {
      display: flex;
      flex-flow: column;
    }
    .color-palette {
    display:flex;
    width: 85%;
    margin: 0 auto;
  }
    .color-shades {
      display: none;
    }
    .color-labels {
      display: none
    }
    .palette b {
      display: inline-block;
    }
    .palette span {
      width: 100%;
    }
  }

  @media all and (-ms-high-contrast:none) {
  .link-list {
      display: -ms-grid;
      -ms-grid-columns: 1fr 1fr;
      -ms-grid-rows: 1fr 1fr;
    }
  }

  .link-item {
    display: block;
    padding: 20px 30px 20px 15px;
    border: 1px solid #00000010;
    border-radius: 5px;
    transition: background 150ms ease-out, border 150ms ease-out, transform 150ms ease-out;
    color: #333333;
    display: flex;
    align-items: flex-start;
  }

  .link-item:hover {
    border-color: #1EA7FD50;
    transform: translate3d(0, -3px, 0);
    box-shadow: rgba(0, 0, 0, 0.08) 0 3px 10px 0;
  }

  .link-item:active {
    border-color: #1EA7FD;
    transform: translate3d(0, 0, 0);
  }

  .link-item strong {
    font-weight: 700;
    display: block;
    margin-bottom: 2px;
  }
  
  .link-item img {
    height: 40px;
    width: 40px;
    margin-right: 15px;
    flex: none;
  }

  .link-item span {
    font-size: 14px;
    line-height: 20px;
  }

  .tip {
    display: inline-block;
    border-radius: 1em;
    font-size: 11px;
    line-height: 12px;
    font-weight: 700;
    background: #E7FDD8;
    color: #66BF3C;
    padding: 4px 12px;
    margin-right: 10px;
    vertical-align: top;
  }

  .tip-wrapper {
    font-size: 13px;
    line-height: 20px;
    margin-top: 40px;
    margin-bottom: 40px;
  }

  .tip-wrapper code {
    font-size: 12px;
    display: inline-block;
  }

  
`}</style>

# Welcome to Storybook

Storybook helps you build UI components in isolation from your app's business logic, data, and context.
That makes it easy to develop hard-to-reach states. Save these UI states as **stories** to revisit during development, testing, or QA.

Browse example stories now by navigating to them in the sidebar.
View their code in the `src/stories` directory to learn how they work.
We recommend building UIs with a [**component-driven**](https://componentdriven.org) process starting with atomic components and ending with pages.

<div className="subheading">Configure</div>

<div className="link-list">
  <Button>buton</Button>
  <Loader type="spin" size={20} />
</div>

<div className="subheading">Color Palette</div>

<ul class="color-shades">
  {Object.keys(colors.red).map((colorShade) => (
    <li key={colorShade}>{colorShade}</li>
  ))}
</ul>
<div className="color-palette">
  <ul class="color-labels">
    {Object.keys(colors).map((colorlabels) => (
      <li key={colorlabels}>{colorlabels}</li>
    ))}
  </ul>
  <div class="palette">
    {Object.entries(colors).map((colorScale, index) => (
      <div key={index}>
        {typeof colorScale[1] == 'object' ? (
          Object.entries(colorScale[1]).map((color) => (
            <span key={color[1]} style={{ background: color[1], color: textColors[colorScale[0]][color[0]] }}>
              <b>
                {colorScale[0]}
                {color[0]}
              </b>
              <i>{color[1]}</i>
            </span>
          ))
        ) : (
          <span key={colorScale[1]} style={{ background: colorScale[1], color: textColors[colorScale[0]] }}>
            <b>{colorScale[0]} </b>
            <i>{colorScale[1]}</i>
          </span>
        )}
      </div>
    ))}
  </div>
</div>
